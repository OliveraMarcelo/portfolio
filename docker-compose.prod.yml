services:
  portfolio:
    image: ${DOCKERHUB_USERNAME}/portfolio:latest
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
